// Wire-aware opt-in marker. Its presence tells a consuming target's build
// plugin to re-parse this library's sources — picking up both bindings and the
// adapter-annotation definitions declared here. Presence-only.
